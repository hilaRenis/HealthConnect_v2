apiVersion: apps/v1
kind: Deployment
metadata:
  name: doctor-service
  namespace: healthconnect
  labels:
    app: doctor-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: doctor-service
  template:
    metadata:
      labels:
        app: doctor-service
        tier: backend
    spec:
      containers:
      - name: doctor-service
        image: gcr.io/PROJECT_ID/doctor-service:latest
        ports:
        - containerPort: 3003
          name: http
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: healthconnect-config
              key: DOCTOR_SERVICE_PORT
        - name: DB_HOST
          value: "doctor-db"
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: healthconnect-config
              key: DB_PORT
        - name: DB_USER
          valueFrom:
            configMapKeyRef:
              name: healthconnect-config
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: healthconnect-secrets
              key: DB_PASSWORD
        - name: DB_NAME
          value: "doctor_service"
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: healthconnect-config
              key: KAFKA_BROKERS
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
